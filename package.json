{
  "name": "lemusicien-fr",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:sites": "astro build && node scripts/prepare-sites-worker.mjs",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^5.12.0"
  }
}
