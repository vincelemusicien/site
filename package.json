{
  "name": "lemusicien-fr",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:sites": "astro build && node scripts/prepare-sites-worker.mjs",
    "preview": "astro preview",
    "check": "astro check",
    "videos:setup": "python3 -m venv .venv-videos && .venv-videos/bin/pip install -r scripts/videos/requirements.txt",
    "videos:sync": "node scripts/videos/refresh.mjs",
    "prebuild": "node scripts/videos/refresh.mjs --optional",
    "prebuild:sites": "node scripts/videos/refresh.mjs --optional"
  },
  "dependencies": {
    "astro": "^5.12.0"
  }
}
